db:
  image: mongo
cron:
  build: ./.docker/cron
  env_file:
    - .secrets/cron.env
  links:
    - web
web:
  build: .
  environment:
    DB_HOST: db
    DB_NAME: dabobert_mdw
  env_file:
    - .secrets/web.env
  ports:
    - "8000:8000"
  links:
    - db
